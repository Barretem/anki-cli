/**
 * 全局用到的常量
 */

/** 默认的anki目录地址 */
export const DEFAULT_DIRNAME_PATH = './anki';

/** 默认的 anki connect 地址 */
export const DEFAULT_ANKI_SERVICE_URL = 'http://127.0.0.1:8765';

/** 默认的根deck值 */
export const DEFAULT_DECK_NAME = 'notes';